// Constructor, every class has one
//

// class Point {
//   x: number;
//   y: number;
//   // if you choose to create a point object with no set values,
//   // make parameters optional, if optional, make sure all to the right are also optional
//   constructor(x?: number, y?: number) {
//     this.x = x;
//     this.y = y;
//   }

//   draw() {
//     console.log('x: ' + this.x, 'y: ' + this.x);
//   }
// }

// let point = new Point(1, 2);
// point.draw();
